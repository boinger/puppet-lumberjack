<%-
hl = []
@hosts.each do |h|
  hl.push "\"#{h}:#{@port}\""
end
host_list = hl.join(',')
-%>
{
  "network": {
    "servers": [<%= host_list %>],
    "ssl ca": "/etc/lumberjack/<%= @name %>/ca.crt",
    "timeout": 15
  },
  "files": [
