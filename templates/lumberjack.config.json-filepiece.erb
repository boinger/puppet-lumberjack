<%-
fields = @fields

if fields
  role = fields['role']
  if role
    if role.is_a? Array
      fields['role'] == role.join(' ')
    end
  end

  extra_fields = fields.map { |k,v| '"' + k + '": "' + v + '"'}.join(",")
else
  extra_fields = ''
end
-%>
    {
      "paths": [ 
        <%= @files.map {|f| '"' + f + '"'}.join(',') %>
      ],
      "fields": { <%= extra_fields %> }
    }
