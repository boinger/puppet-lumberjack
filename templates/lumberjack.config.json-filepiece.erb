<%-
fields = @fields
if fields
  extra_fields = fields.map { |k,v| '"' + k + '": "' + v + '"'}.join(",")
else
  extra_fields = ''
end
-%>
    {
      "paths": [ 
        "<%= @files.join(' ') %>"
      ],
      "fields": { <%= @extra_fields %> }
    }
